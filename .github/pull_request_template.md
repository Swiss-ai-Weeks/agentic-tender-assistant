## Track
<!-- A (ingestion) / B (eligibility + fit scoring) / C (briefing + orchestration) / other -->

## What changed

## Tests run
<!-- e.g. `pytest`, `python -m src.pipeline ...`, `nat run --config_file ...` -->

## Checklist
- [ ] `pytest` passes locally
- [ ] `ruff check .` passes (or `pre-commit run --all-files`)
- [ ] No secrets / `.env` committed
